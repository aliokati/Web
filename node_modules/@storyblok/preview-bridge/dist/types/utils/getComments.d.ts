import { EditableAttributes } from '../editable/parsing';
/**
 * Convert a string to editable attribute data.
 */
export declare const returnParsedJson: (value: string) => Partial<EditableAttributes> | null;
type StoryblokComment = {
    options: Partial<EditableAttributes>;
    el: HTMLElement | null;
};
/**
 * Get Storyblok comments contained within a DOM node.
 */
export declare const getComments: (rootElement: Node) => StoryblokComment[];
export {};
