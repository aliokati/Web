import { EditableBlockData } from '../BlockIframeData';
/**
 * Find the closest element with the specified attribute.
 */
export declare const closest: (el: Element | null, selector: string) => Element | null;
/**
 * get the data-blok-c information from the current node or it's closest parent
 */
export declare const getBlokInfoFromNode: (element: Element | null) => EditableBlockData | null;
export declare const isDescendant: (element: Node, elementToCheck: Node | null) => boolean;
/**
 * get the specific element in the dom using the id + uid,
 * if the element not find the `null` is returned.
 */
export declare const getBlokInDocument: (fullId: string) => Element | null;
export declare const elementInViewport: (el: Element) => boolean;
