import { BridgeEvent } from './BridgeEvent';
export type BridgeParams = {
    customParent?: string | null;
    resolveRelations?: string[] | null;
    resolveLinks?: 'url' | 'story' | '0' | '1' | null;
    preventClicks?: boolean;
    initOnlyOnce?: boolean;
    fallbackLang?: string | null;
};
/**
 * Define public methods to implement a Bridge between a website and
 * Storyblok Editor
 */
export default class StoryblokBridge {
    /**
     * Create an instance of StoryblokBridge
     * @param props
     */
    constructor(props?: BridgeParams);
    /**
     * Indicate if the website is inside the Storyblok Editor
     *
     * @method isInEditor
     * @return
     */
    isInEditor: () => boolean;
    /**
     * Enter in editMode
     *
     * @method enterEditmode
     * @param event
     */
    enterEditmode: (event: {}) => void;
    /**
     * Ping the Storyblok Editor
     *
     * @method pingEditor
     * @param  callback function that gets called and receives the class instance as argument
     */
    pingEditor: (callback: (this: unknown) => void) => void;
    /**
     * Subscribe a listener to an event from Storyblok Editor
     *
     * @method on
     * @param  event event or a set of events names
     * @param callback callback function
     */
    on: <Action extends BridgeEvent['action']>(event: Action | Action[], callback: (this: unknown, payload: Extract<BridgeEvent, {
        action: Action;
    }>) => void) => void;
    /**
     * Tear down the bridge: remove its event listeners, timers and injected DOM
     *
     * @method destroy
     */
    destroy: () => void;
}
