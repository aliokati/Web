export type EditableHtmlElementAttributes = {
    'data-blok-c': string;
    'data-blok-uid': string;
};
/**
 * Make an `HTMLElement` in your embedded application interactive by including
 * @param content
 * @example
 * With React:
 * ```tsx
 * const HeroContent = (props: { content: HeroContent }) => (
 *   <div {{...editableDomElementAttributes(props.content)}}>
 *     <h1>{props.content.title}</h1>
 *   </div>
 * )
 * ```
 * @example
 * With Vue:
 * ```vue
 * <template>
 *    <div v-bind="editableDomElementAttributes(content)">
 *      <h1>{content.title}</h1>
 *    </div>
 * </template>
 * ```
 */
export declare const editableAttributes: (content: {
    _editable?: string;
}) => EditableHtmlElementAttributes | {};
