import { Parser } from 'pure-parse';
export type EditableAttributes = {
    name: string;
    space?: string;
    uid: string;
    id: string;
};
/**
 * Parses the Json value from the string value in the `_editable` property.
 * @param content
 */
export declare const parseEditableAttributesFromString: Parser<EditableAttributes>;
