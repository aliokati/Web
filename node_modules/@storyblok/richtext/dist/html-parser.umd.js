(function(global, factory) {
	typeof exports === "object" && typeof module !== "undefined" ? factory(exports, require("@tiptap/html"), require("@tiptap/extension-emoji"), require("@tiptap/core"), require("@tiptap/extension-list"), require("@tiptap/extension-table"), require("@tiptap/extension-blockquote"), require("@tiptap/extension-code-block"), require("@tiptap/extension-document"), require("@tiptap/extension-hard-break"), require("@tiptap/extension-heading"), require("@tiptap/extension-horizontal-rule"), require("@tiptap/extension-image"), require("@tiptap/extension-paragraph"), require("@tiptap/extension-text"), require("@tiptap/extension-bold"), require("@tiptap/extension-code"), require("@tiptap/extension-highlight"), require("@tiptap/extension-italic"), require("@tiptap/extension-link"), require("@tiptap/extension-strike"), require("@tiptap/extension-subscript"), require("@tiptap/extension-superscript"), require("@tiptap/extension-underline")) : typeof define === "function" && define.amd ? define([
		"exports",
		"@tiptap/html",
		"@tiptap/extension-emoji",
		"@tiptap/core",
		"@tiptap/extension-list",
		"@tiptap/extension-table",
		"@tiptap/extension-blockquote",
		"@tiptap/extension-code-block",
		"@tiptap/extension-document",
		"@tiptap/extension-hard-break",
		"@tiptap/extension-heading",
		"@tiptap/extension-horizontal-rule",
		"@tiptap/extension-image",
		"@tiptap/extension-paragraph",
		"@tiptap/extension-text",
		"@tiptap/extension-bold",
		"@tiptap/extension-code",
		"@tiptap/extension-highlight",
		"@tiptap/extension-italic",
		"@tiptap/extension-link",
		"@tiptap/extension-strike",
		"@tiptap/extension-subscript",
		"@tiptap/extension-superscript",
		"@tiptap/extension-underline"
	], factory) : (global = typeof globalThis !== "undefined" ? globalThis : global || self, factory(global.StoryblokRichtextHtmlParser = {}, global.TiptapHtml, global.TiptapEmoji, global.TiptapCore, global.TiptapList, global.TiptapTable, global.TiptapBlockquote, global.TiptapCodeBlock, global.TiptapDocument, global.TiptapHardBreak, global.TiptapHeading, global.TiptapHorizontalRule, global.TiptapImage, global.TiptapParagraph, global.TiptapText, global.TiptapBold, global.TiptapCode, global.TiptapHighlight, global.TiptapItalic, global.TiptapLink, global.TiptapStrike, global.TiptapSubscript, global.TiptapSuperscript, global.TiptapUnderline));
})(this, function(exports, _tiptap_html, _tiptap_extension_emoji, _tiptap_core, _tiptap_extension_list, _tiptap_extension_table, _tiptap_extension_blockquote, _tiptap_extension_code_block, _tiptap_extension_document, _tiptap_extension_hard_break, _tiptap_extension_heading, _tiptap_extension_horizontal_rule, _tiptap_extension_image, _tiptap_extension_paragraph, _tiptap_extension_text, _tiptap_extension_bold, _tiptap_extension_code, _tiptap_extension_highlight, _tiptap_extension_italic, _tiptap_extension_link, _tiptap_extension_strike, _tiptap_extension_subscript, _tiptap_extension_superscript, _tiptap_extension_underline) {
	Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
	//#region \0rolldown/runtime.js
	var __create = Object.create;
	var __defProp = Object.defineProperty;
	var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
	var __getOwnPropNames = Object.getOwnPropertyNames;
	var __getProtoOf = Object.getPrototypeOf;
	var __hasOwnProp = Object.prototype.hasOwnProperty;
	var __copyProps = (to, from, except, desc) => {
		if (from && typeof from === "object" || typeof from === "function") for (var keys = __getOwnPropNames(from), i = 0, n = keys.length, key; i < n; i++) {
			key = keys[i];
			if (!__hasOwnProp.call(to, key) && key !== except) __defProp(to, key, {
				get: ((k) => from[k]).bind(null, key),
				enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable
			});
		}
		return to;
	};
	var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(isNodeMode || !mod || !mod.__esModule || !__hasOwnProp.call(mod, "default") ? __defProp(target, "default", {
		value: mod,
		enumerable: true
	}) : target, mod));
	//#endregion
	_tiptap_extension_emoji = __toESM(_tiptap_extension_emoji, 1);
	_tiptap_extension_blockquote = __toESM(_tiptap_extension_blockquote, 1);
	_tiptap_extension_code_block = __toESM(_tiptap_extension_code_block, 1);
	_tiptap_extension_document = __toESM(_tiptap_extension_document, 1);
	_tiptap_extension_hard_break = __toESM(_tiptap_extension_hard_break, 1);
	_tiptap_extension_heading = __toESM(_tiptap_extension_heading, 1);
	_tiptap_extension_horizontal_rule = __toESM(_tiptap_extension_horizontal_rule, 1);
	_tiptap_extension_image = __toESM(_tiptap_extension_image, 1);
	_tiptap_extension_paragraph = __toESM(_tiptap_extension_paragraph, 1);
	_tiptap_extension_text = __toESM(_tiptap_extension_text, 1);
	_tiptap_extension_bold = __toESM(_tiptap_extension_bold, 1);
	_tiptap_extension_code = __toESM(_tiptap_extension_code, 1);
	_tiptap_extension_highlight = __toESM(_tiptap_extension_highlight, 1);
	_tiptap_extension_italic = __toESM(_tiptap_extension_italic, 1);
	_tiptap_extension_link = __toESM(_tiptap_extension_link, 1);
	_tiptap_extension_strike = __toESM(_tiptap_extension_strike, 1);
	_tiptap_extension_subscript = __toESM(_tiptap_extension_subscript, 1);
	_tiptap_extension_superscript = __toESM(_tiptap_extension_superscript, 1);
	_tiptap_extension_underline = __toESM(_tiptap_extension_underline, 1);
	//#region src/static/style.ts
	/**
	* Converts a CSS string to a style object.
	* @param style - The CSS string to convert.
	* @returns A style object representation of the CSS string.
	* @example
	* const cssString = "color: red; font-size: 16px";
	* const styleObj = stringToStyle(cssString);
	* console.log(styleObj); // Output: { color: 'red', fontSize: '16px' }
	*/
	function stringToStyle(style) {
		return style.split(";").map((rule) => rule.trim()).filter(Boolean).reduce((acc, rule) => {
			const colonIdx = rule.indexOf(":");
			if (colonIdx === -1) return acc;
			const key = rule.slice(0, colonIdx).trim();
			const value = rule.slice(colonIdx + 1).trim();
			if (!key || !value) return acc;
			acc[kebabToCamel(key)] = value;
			return acc;
		}, {});
	}
	function kebabToCamel(str) {
		return str.replace(/-([a-z])/g, (_, c) => c.toUpperCase());
	}
	//#endregion
	//#region src/extensions/utils.ts
	/**
	* List of supported HTML attributes by tag name, used by the Reporter mark.
	*/
	const supportedAttributesByTagName = {
		a: [
			"href",
			"target",
			"data-uuid",
			"data-anchor",
			"data-linktype"
		],
		img: [
			"alt",
			"src",
			"title"
		],
		span: ["class"]
	};
	/**
	* Creates a DOM attribute parser for use in rich text extensions.
	*
	* The returned function extracts a value from an element by:
	* 1. Checking one or more HTML attributes (in order).
	* 2. Also can map to CSS inline style property.
	*
	* The first non-empty value found is returned.
	*
	* Example:
	* <div data-source="example" data-copyright="2024"></div>
	* attributeParsers: {
	*   source: mapToAttribute('data-source'),
	*   copyright: mapToAttribute(['data-copyright', 'data-rights']),
	* }
	* Style Example:
	* <div name="example" style="color: red; font-size: 16px;"></div>
	* attributeParsers: {
	*   name: mapToAttribute('name'),
	*   color: mapToAttribute(undefined, 'color'),
	*   fontSize: mapToAttribute(undefined, 'font-size'),
	* }
	*
	* @param attributes - A single attribute name or list of attribute names to read from.
	* @param styleProperty - Optional CSS property name to read from the element's inline style.
	* @returns A function that receives an HTMLElement and returns the resolved value or null.
	*/
	function mapToAttribute(attributes, styleProperty) {
		return (el) => {
			if (attributes) {
				const attrs = Array.isArray(attributes) ? attributes : [attributes];
				for (const name of attrs) {
					const value = el.getAttribute(name);
					if (value) return value;
				}
			}
			if (styleProperty) {
				const style = el.getAttribute("style");
				if (style) {
					const value = stringToStyle(style)[kebabToCamel(styleProperty)];
					if (value) return value;
				}
			}
			return null;
		};
	}
	//#endregion
	//#region src/extensions/nodes.ts
	function buildEmojiExtension(options) {
		const parser = options?.attributeParsers;
		return _tiptap_extension_emoji.default.extend({
			parseHTML: options?.parseHTML ?? (() => {
				return [{ tag: "img[data-emoji]" }, { tag: "span[data-emoji]" }];
			}),
			addAttributes() {
				return {
					fallbackImage: {
						default: null,
						parseHTML: parser?.fallbackImage ?? mapToAttribute(["src", "data-src"])
					},
					name: {
						default: null,
						parseHTML: parser?.name ?? mapToAttribute("data-name")
					},
					emoji: {
						default: null,
						parseHTML: parser?.emoji ?? mapToAttribute("data-emoji")
					}
				};
			},
			renderHTML({ HTMLAttributes }) {
				return [
					"span",
					{
						"data-emoji": HTMLAttributes.emoji,
						"data-name": HTMLAttributes.name
					},
					HTMLAttributes.emoji
				];
			}
		});
	}
	function buildBlockquoteExtension() {
		return _tiptap_extension_blockquote.default.extend({ renderHTML({ HTMLAttributes }) {
			return [
				"blockquote",
				{ ...HTMLAttributes },
				0
			];
		} });
	}
	function buildParagraphExtension(options) {
		const parser = options?.attributeParsers;
		return _tiptap_extension_paragraph.default.extend({ addAttributes() {
			return { textAlign: {
				default: null,
				parseHTML: parser?.textAlign || mapToAttribute(void 0, "text-align")
			} };
		} });
	}
	function buildHeadingExtension(options) {
		const parser = options?.attributeParsers;
		return _tiptap_extension_heading.default.extend({ addAttributes() {
			return {
				level: {
					default: 1,
					parseHTML: parser?.level
				},
				textAlign: {
					default: null,
					parseHTML: parser?.textAlign || mapToAttribute(void 0, "text-align")
				}
			};
		} });
	}
	function buildTableRowExtension() {
		return _tiptap_extension_table.TableRow.extend({ renderHTML({ HTMLAttributes }) {
			return [
				"tr",
				{ ...HTMLAttributes },
				0
			];
		} });
	}
	function buildBulletListExtension() {
		return _tiptap_extension_list.BulletList.extend({
			name: "bullet_list",
			addOptions() {
				return {
					...this.parent(),
					itemTypeName: "list_item"
				};
			}
		});
	}
	function buildOrderedListExtension(options) {
		const parser = options?.attributeParsers;
		return _tiptap_extension_list.OrderedList.extend({
			name: "ordered_list",
			addAttributes() {
				return { order: {
					default: 1,
					parseHTML: parser?.order || ((el) => {
						const value = el.getAttribute("start") || el.getAttribute("data-order");
						if (value) {
							const num = Number(value);
							if (!Number.isNaN(num)) return num;
						}
						return 1;
					})
				} };
			},
			addOptions() {
				return {
					...this.parent(),
					itemTypeName: "list_item"
				};
			}
		});
	}
	function buildListItemExtension() {
		return _tiptap_extension_list.ListItem.extend({
			name: "list_item",
			addOptions() {
				return {
					...this.parent(),
					bulletListTypeName: "bullet_list",
					orderedListTypeName: "ordered_list"
				};
			}
		});
	}
	function buildCodeBlockExtension(options) {
		const parser = options?.attributeParsers;
		return _tiptap_extension_code_block.default.extend({
			name: "code_block",
			addAttributes() {
				return { class: {
					default: null,
					parseHTML: parser?.class || ((el) => {
						const code = el.querySelector("code");
						if (code) return code.getAttribute("data-language") || code.getAttribute("class");
						return el.getAttribute("data-language") || el.getAttribute("class");
					})
				} };
			},
			renderHTML({ HTMLAttributes }) {
				return ["pre", [
					"code",
					{ class: HTMLAttributes.class },
					0
				]];
			}
		});
	}
	function buildHardBreakExtension() {
		return _tiptap_extension_hard_break.default.extend({ name: "hard_break" });
	}
	function buildHorizontalRuleExtension() {
		return _tiptap_extension_horizontal_rule.default.extend({ name: "horizontal_rule" });
	}
	function buildTableExtension() {
		return _tiptap_extension_table.Table.extend({ renderHTML({ HTMLAttributes }) {
			return [
				"table",
				{ ...HTMLAttributes },
				0
			];
		} });
	}
	const parseTableWidth = (element) => {
		const colwidth = element.getAttribute("colwidth");
		if (colwidth) {
			const values = colwidth.split(",").map((v) => Number(v.trim())).filter((v) => !Number.isNaN(v));
			return values.length ? values : null;
		}
		const styleWidth = element.style?.width;
		if (styleWidth) {
			const numeric = Number.parseFloat(styleWidth);
			if (!Number.isNaN(numeric)) return [numeric];
		}
		return null;
	};
	function buildTableCellExtension(options) {
		const parser = options?.attributeParsers;
		return _tiptap_extension_table.TableCell.extend({ addAttributes() {
			return {
				colspan: {
					default: 1,
					parseHTML: parser?.colspan
				},
				rowspan: {
					default: 1,
					parseHTML: parser?.rowspan
				},
				colwidth: {
					default: null,
					parseHTML: parser?.colwidth ?? parseTableWidth
				},
				backgroundColor: {
					default: null,
					parseHTML: parser?.backgroundColor ?? mapToAttribute(void 0, "background-color")
				}
			};
		} });
	}
	function buildTableHeaderExtension(options) {
		const parser = options?.attributeParsers;
		return _tiptap_extension_table.TableHeader.extend({ addAttributes() {
			return {
				colspan: {
					default: 1,
					parseHTML: parser?.colspan
				},
				rowspan: {
					default: 1,
					parseHTML: parser?.rowspan
				},
				colwidth: {
					default: null,
					parseHTML: parser?.colwidth ?? parseTableWidth
				}
			};
		} });
	}
	function buildImageExtension(options) {
		const parser = options?.attributeParsers;
		return _tiptap_extension_image.default.extend({ addAttributes() {
			return {
				id: {
					default: null,
					parseHTML: parser?.id
				},
				src: {
					default: null,
					parseHTML: parser?.src
				},
				alt: {
					default: null,
					parseHTML: parser?.alt
				},
				title: {
					default: null,
					parseHTML: parser?.title
				},
				source: {
					default: null,
					parseHTML: parser?.source
				},
				copyright: {
					default: null,
					parseHTML: parser?.copyright
				},
				meta_data: {
					default: null,
					parseHTML: parser?.meta_data
				}
			};
		} });
	}
	function buildComponentBlokExtension(options) {
		const parser = options?.attributeParsers;
		return _tiptap_core.Node.create({
			name: "blok",
			group: "block",
			atom: true,
			addAttributes() {
				return {
					id: {
						default: null,
						parseHTML: parser?.id
					},
					body: {
						default: [],
						parseHTML: parser?.body
					}
				};
			},
			parseHTML: options?.parseHTML ?? (() => {
				return [{ tag: "div[data-blok]" }];
			})
		});
	}
	//#endregion
	//#region src/extensions/marks.ts
	function buildHighlightExtension(options) {
		const parser = options?.attributeParsers;
		return _tiptap_extension_highlight.default.extend({ addAttributes() {
			return { color: {
				default: null,
				parseHTML: parser?.color ?? mapToAttribute(void 0, "color")
			} };
		} });
	}
	const parseCustomLinkAttributes = (element) => {
		const defaultLinkAttributes = supportedAttributesByTagName.a;
		const customAttributeNames = element.getAttributeNames().filter((n) => !defaultLinkAttributes.includes(n));
		const customAttributes = {};
		for (const attributeName of customAttributeNames) customAttributes[attributeName] = element.getAttribute(attributeName);
		return Object.keys(customAttributes).length ? customAttributes : null;
	};
	function buildLinkExtension(options) {
		const parser = options?.attributeParsers;
		return _tiptap_extension_link.default.extend({ addAttributes() {
			return {
				href: { parseHTML: parser?.href ?? mapToAttribute("href") },
				uuid: {
					default: null,
					parseHTML: parser?.uuid ?? mapToAttribute("data-uuid")
				},
				anchor: {
					default: null,
					parseHTML: parser?.anchor ?? mapToAttribute("data-anchor")
				},
				target: {
					default: null,
					parseHTML: parser?.target ?? mapToAttribute("target")
				},
				linktype: {
					default: "url",
					parseHTML: parser?.linktype ?? mapToAttribute("data-linktype")
				},
				custom: {
					default: {},
					parseHTML: parser?.custom ?? parseCustomLinkAttributes
				}
			};
		} });
	}
	function buildAnchorExtension(options) {
		const parser = options?.attributeParsers;
		return _tiptap_core.Mark.create({
			name: "anchor",
			addAttributes() {
				return { id: {
					default: null,
					parseHTML: parser?.id ?? mapToAttribute(["id", "data-id"])
				} };
			},
			parseHTML: options?.parseHTML ?? (() => {
				return [{ tag: "span[id]" }];
			}),
			renderHTML({ HTMLAttributes }) {
				const { id } = HTMLAttributes;
				return [
					"span",
					{ id },
					0
				];
			}
		});
	}
	function buildStyledExtension(options) {
		const parser = options?.attributeParsers;
		return _tiptap_core.Mark.create({
			name: "styled",
			parseHTML: options?.parseHTML ?? (() => {
				return [{
					tag: "span",
					consuming: false,
					getAttrs: (element) => {
						if (element.getAttribute("class")) return null;
						return false;
					}
				}];
			}),
			addAttributes() {
				return { class: {
					default: null,
					parseHTML: parser?.class ?? mapToAttribute("class")
				} };
			},
			renderHTML({ HTMLAttributes }) {
				const { class: className } = HTMLAttributes;
				return [
					"span",
					{ class: className },
					0
				];
			}
		});
	}
	function buildTextStyleExtension(options) {
		const parser = options?.attributeParsers;
		return _tiptap_core.Mark.create({
			name: "textStyle",
			parseHTML: options?.parseHTML ?? (() => {
				return [{
					tag: "span",
					consuming: false,
					getAttrs: (element) => {
						const style = element.getAttribute("style");
						if (style && /color/i.test(style)) return null;
						return false;
					}
				}];
			}),
			addAttributes() {
				return { color: {
					default: null,
					parseHTML: parser?.color ?? mapToAttribute(void 0, "color")
				} };
			},
			renderHTML({ HTMLAttributes }) {
				const { color } = HTMLAttributes;
				const styles = [];
				if (color) styles.push(`color: ${color};`);
				return [
					"span",
					styles.length > 0 ? { style: styles.join(" ") } : {},
					0
				];
			}
		});
	}
	const Reporter = _tiptap_core.Mark.create({
		name: "reporter",
		priority: 0,
		addOptions() {
			return { allowCustomAttributes: false };
		},
		parseHTML() {
			return [{
				tag: "*",
				consuming: false,
				getAttrs: (element) => {
					const tagName = element.tagName.toLowerCase();
					if (tagName === "a" && this.options.allowCustomAttributes) return false;
					const unsupportedAttributes = element.getAttributeNames().filter((attr) => {
						return !(tagName in supportedAttributesByTagName ? supportedAttributesByTagName[tagName] : []).includes(attr);
					});
					for (const attr of unsupportedAttributes) console.warn(`[StoryblokRichText] - \`${attr}\` "${element.getAttribute(attr)}" on \`<${tagName}>\` can not be transformed to rich text.`);
					return false;
				}
			}];
		}
	});
	//#endregion
	//#region src/extensions/index.ts
	function getStoryblokTiptapExtensions(options) {
		const parsers = options?.parsers || {};
		const enableReporter = options?.enableReporter || false;
		return {
			document: _tiptap_extension_document.default,
			text: _tiptap_extension_text.default,
			emoji: buildEmojiExtension(parsers?.emoji),
			paragraph: buildParagraphExtension(parsers?.paragraph),
			blockquote: buildBlockquoteExtension(),
			heading: buildHeadingExtension(parsers?.heading),
			bulletList: buildBulletListExtension(),
			orderedList: buildOrderedListExtension(parsers?.ordered_list),
			listItem: buildListItemExtension(),
			codeBlock: buildCodeBlockExtension(parsers?.code_block),
			hardBreak: buildHardBreakExtension(),
			horizontalRule: buildHorizontalRuleExtension(),
			image: buildImageExtension(parsers?.image),
			table: buildTableExtension(),
			tableRow: buildTableRowExtension(),
			tableCell: buildTableCellExtension(parsers?.tableCell),
			tableHeader: buildTableHeaderExtension(parsers?.tableHeader),
			blok: buildComponentBlokExtension(parsers?.blok),
			bold: _tiptap_extension_bold.default,
			italic: _tiptap_extension_italic.default,
			strike: _tiptap_extension_strike.default,
			underline: _tiptap_extension_underline.default,
			code: _tiptap_extension_code.default,
			superscript: _tiptap_extension_superscript.default,
			subscript: _tiptap_extension_subscript.default,
			highlight: buildHighlightExtension(parsers?.highlight),
			textStyle: buildTextStyleExtension(parsers?.textStyle),
			link: buildLinkExtension(parsers?.link),
			anchor: buildAnchorExtension(parsers?.anchor),
			styled: buildStyledExtension(parsers?.styled),
			...enableReporter && { reporter: Reporter }
		};
	}
	//#endregion
	//#region src/html-parser.ts
	/**
	* Converts an HTML string into a Storyblok Rich Text JSON document.
	*
	* This function:
	* - Parses the provided HTML using the configured Storyblok Tiptap extensions
	* - Returns a Storyblok-compatible rich text document structure
	*
	* By default, whitespace is not preserved during parsing.
	*
	* @param html - The HTML string to convert.
	* @param options - Optional configuration for parsing behavior.
	* @param options.parsers - Custom parser configuration for node/mark types.
	* @param options.preserveWhitespace - Whether to preserve whitespace during parsing.
	*
	* @returns A Storyblok Rich Text JSON document.
	*
	* @example
	* const result = htmlToStoryblokRichtext(html, {
	*   parsers: {
	*     image: {
	*       attributeParsers: {
	*         source: mapToAttribute('data-source'),
	*         copyright: mapToAttribute('data-copyright'),
	*       },
	*     },
	*   },
	* });
	*
	* console.log(result);
	*/
	function htmlToStoryblokRichtext(html, options) {
		const allExtensions = getStoryblokTiptapExtensions({
			parsers: options?.parsers,
			enableReporter: true
		});
		return (0, _tiptap_html.generateJSON)(html, Object.values(allExtensions), { preserveWhitespace: options?.preserveWhitespace || false });
	}
	//#endregion
	exports.htmlToStoryblokRichtext = htmlToStoryblokRichtext;
	exports.mapToAttribute = mapToAttribute;
});

//# sourceMappingURL=html-parser.umd.js.map